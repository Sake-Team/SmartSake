flask>=3.0
gunicorn

# Pi-only (not needed for mock_server dev mode)
# RPi.GPIO        # fan relay control, HX711 load cell bit-bang
# hx711           # load_cell_hx711.py (custom driver, uses RPi.GPIO directly)
